# frozen_string_literal: true

RSpec.describe Marmerdo do
  it "has a version number" do
    expect(Marmerdo::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
